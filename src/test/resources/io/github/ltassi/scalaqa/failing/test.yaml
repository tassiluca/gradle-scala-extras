tests:
  - description: "Malformatted sources"
    configuration:
      tasks:
        - clean
        - check
      options:
        - --stacktrace
        - --info
        - --continue
    expectation:
      result: failed
      outcomes:
        failed:
          - checkScalafmt
          - checkTestScalafmt
#          - checkScalafixMain
#          - checkScalafixTest
#      output:
#        contains:
#          # IMPORTANT: whitespaces are significant in these snippets
#          # Scalafmt
#          - |
#            src/main/scala/Main.scala
#                      @@ -1,6 +1,5 @@
#                       import·scala.Nil
#
#                      -@main·def
#                      -helloWorld():·Unit·=
#                      +@main·def·helloWorld():·Unit·=
#          - |
#            src/test/scala/MainTest.scala
#                      @@ -2,8 +2,7 @@
#                       import·org.scalatest.matchers.should.Matchers
#                       import·scala.List
#
#                      -class
#                      -MainTest·extends·AnyFlatSpec·with·Matchers:
#                      +class·MainTest·extends·AnyFlatSpec·with·Matchers:
