tests:
  - description: "With custom configuration"
    configuration:
      tasks:
        - clean
        - check
      options:
        - --stacktrace
        - --info
    expectation:
      result: success
      outcomes:
        success:
          - spotlessScalaCheck
      output:
        contains:
          - "Using scalafmt version 3.0.0"