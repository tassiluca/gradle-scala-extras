tests:
  - description: "Scala QA plugin applied to an empty project"
    configuration:
      tasks:
        - build
      options:
        - --stacktrace
    expectation:
      result: success
      outcomes:
        success:
          - build
